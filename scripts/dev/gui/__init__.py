"""Dev-loop inspection GUI package.

Streamlit-native, browser-rendered, runs locally only.
Scope: dev-loop only — never on the reportable artifact path.
See `_bmad-output/planning-artifacts/ux-design-specification.md`.
"""
